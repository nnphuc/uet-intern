<h1>Home#index</h1>
<p>Find me in app/views/home/index.html.erb</p>

<% if current_user and current_user.has_role? :student %>

    <%= image_tag(current_user.student_info.avatar.url(:medium)) %> 
<% elsif  current_user and current_user.has_role? :lecturer %>
    <%= image_tag(current_user.lecturer_info.avatar.url(:medium)) %> 
<% end %>